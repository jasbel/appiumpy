class AX:
    class TabTop:
        BASE = "top"
        BACK = f"{BASE}-back"
        TITLE = f"{BASE}-title"
        RIGHT = f"{BASE}-action-right"

    class TabBottom:
        BASE = "tab"
        HOME = f"{BASE}-home"
        TEMPORARY = f"{BASE}-temporary"
        TRANSACTIONS = f"{BASE}-transactions"
        OPTIONS = f"{BASE}-options"

    class Home:
        BASE = "home"
        TAB_NFC = f"{BASE}-tab-nfc"
        TAB_MOBILE = f"{BASE}-tab-mobile"
        BOX_TRANSACTIONS = f"{BASE}-box-transactions"

    class Options:
        BASE = "option"
        ITEM_LANGUAGE = f"{BASE}-language"
        SELECT_LANGUAGE = f"{BASE}-select-language"
        PROOFS = f"{BASE}-proofs"
        PROOFS_TOGGLE = f"{BASE}-proofs-toggle"
        MAPS = f"{BASE}-maps"
        TEMPORARY_TITLES = f"{BASE}-temporary-titles"
        PLANNER = f"{BASE}-planner"
        ABOUT_THE_APP = f"{BASE}-about"
        PRIVACY_POLICY = f"{BASE}-privacy"
